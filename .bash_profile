#
# ~/.bash_profile
#
# Executed once on login
# Variables set here are not available to the graphical environment
#

[ -f ~/.profile ] && . ~/.profile
[ -f ~/.bash_profile_local ] && . ~/.bash_profile_local
[ -f ~/.bashrc ] && . ~/.bashrc
